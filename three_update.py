"""兼容保留模块。

旧版项目使用此文件在多个文件夹之间复制 DLL。现在所有 DLL 位于同一个文件夹，
不再需要更新步骤；仍保留 run()，避免其他旧代码 import 本模块时出错。
"""

import logger


def run():
    """不执行文件操作，只在日志中说明单文件夹模式无需更新。"""
    logger.log("单文件夹模式：无需执行更新")


if __name__ == "__main__":
    run()
