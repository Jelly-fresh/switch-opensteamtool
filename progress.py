"""兼容保留模块。

旧版项目在切换时显示 Tkinter 进度窗口。现在按需求取消提示窗口，
保留此文件和 run() 函数只是为了兼容可能还在引用它的旧代码。
"""

import logger


def run():
    """不显示窗口，只记录一次兼容调用。"""
    logger.log("切换操作已执行")


if __name__ == "__main__":
    run()
